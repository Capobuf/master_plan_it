## Scope
Implement MPIT v16 sidebar/navigation preflight outcomes with minimal native Frappe changes: stale test alignment, app-level metadata, /apps exposure hook, and one idempotent cleanup patch.

## Files read
- Re-read requested MPIT files: `AGENT_INSTRUCTIONS.md`, `hooks.py`, `config/desktop.py`, `setup/install.py`, tests, workspace JSON, `patches.txt`.
- Re-verified live Frappe source in container: `frappe/model/sync.py`, `workspace_sidebar`/`desktop_icon`/`sidebar_item_group` doctypes and shipped app-level JSON.

## Proven facts
- Canonical app-level sync path in this environment is flat under `frappe.get_app_path("master_plan_it")`:
  - `master_plan_it/workspace_sidebar/<name>.json`
  - `master_plan_it/desktop_icon/<name>.json`
  - `master_plan_it/sidebar_item_group/<name>.json`
- `Workspace Sidebar Item` is a child table in `Workspace Sidebar` JSON.
- Existing non-standard legacy records were present before implementation and removed by patch.
- Standard MPIT records now present after migrate:
  - `Workspace Sidebar` "Master Plan IT" (`standard=1`, `app=master_plan_it`)
  - `Desktop Icon` "Master Plan IT" (`standard=1`, `icon_type=App`)
- `/apps` hook now exposes MPIT with route `/app/master-plan-it`.
- Curated sidebar labels match target model and include no top-level `Plafond`.

## Conflicts
- Stale test assumptions were corrected:
  - `workspace_sidebar` path was treated as forbidden in generic path test.
  - sidebar canonical-path test expected nested `{name}/{name}.json`.

## Questions
- none

## Proposed implementation surface
- `master_plan_it/hooks.py`
- `master_plan_it/tests/test_no_forbidden_metadata_paths.py`
- `master_plan_it/patches.txt`
- `master_plan_it/patches/v1_0/remove_legacy_mpit_navigation_records.py`
- `master_plan_it/workspace_sidebar/master_plan_it.json`
- `master_plan_it/desktop_icon/master_plan_it.json`
