import{useState}from"react";import{changeOwnPassword}from"../../api/auth";import{ApiError}from"../../api/client";import ComponentCard from"../common/ComponentCard";import Label from"../form/Label";import InputField from"../form/input/InputField";import Alert from"../ui/alert/Alert";import Button from"../ui/button/Button";
export default function ChangePasswordForm(){const[current,setCurrent]=useState(""),[password,setPassword]=useState(""),[confirmation,setConfirmation]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState<string|null>(null),[error,setError]=useState<string|null>(null);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError(null);setMessage(null);try{await changeOwnPassword(current,password,confirmation);setCurrent("");setPassword("");setConfirmation("");setMessage("Password modificata. Accedi nuovamente con la nuova password.");}catch(err){setError(ApiError.from(err).message)}finally{setBusy(false)}};return <ComponentCard title="Cambia password">{error?<Alert variant="error" title="Modifica non riuscita" message={error}/>:null}{message?<Alert variant="success" title="Password aggiornata" message={message}/>:null}<form onSubmit={submit} className="space-y-4"><div><Label>Password corrente</Label><InputField type="password" value={current} onChange={e=>setCurrent(e.target.value)} autoComplete="current-password"/></div><div><Label>Nuova password</Label><InputField type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/></div><div><Label>Conferma nuova password</Label><InputField type="password" value={confirmation} onChange={e=>setConfirmation(e.target.value)} autoComplete="new-password"/></div><Button disabled={busy||!current||!password||!confirmation}>{busy?"Salvataggio…":"Salva password"}</Button></form></ComponentCard>}
