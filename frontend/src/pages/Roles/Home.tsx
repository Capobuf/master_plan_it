import { useApplicationContext } from "../../context/ApplicationContext"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import RolesView from "../../components/roles/RolesView";
export default function RolesHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const view = hasAbility("tenant-roles.view")||hasAbility("tenant-roles.manage"); const manage = hasAbility("tenant-roles.manage"); return <><PageMeta title="Ruoli | Master Plan IT" description="Ruoli e autorizzazioni del Tenant" />{loading ? <Alert variant="info" title="Caricamento del contesto" message="Verifica del Tenant in corso." /> : tenant === null ? <Alert variant="warning" title="Tenant richiesto" message="Seleziona un Tenant prima di gestire i ruoli." /> : !view ? <Alert variant="warning" title="Ruoli non disponibili" message="Non disponi dell'autorizzazione necessaria per visualizzare questa pagina." /> : <RolesView canView={view} canManage={manage} />}</>; }
