import { useApplicationContext } from "../../context/ApplicationContext"; import PageBreadcrumb from "../../components/common/PageBreadCrumb"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import RolesView from "../../components/roles/RolesView";
export default function RolesHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const manage = hasAbility("platform.roles.manage"); return <><PageMeta title="Roles | Master Plan IT" description="Tenant roles and abilities" /><PageBreadcrumb pageTitle="Roles" />{loading ? <Alert variant="info" title="Loading application context" message="Checking tenant and role access." /> : tenant === null ? <Alert variant="warning" title="Tenant required" message="Enter a tenant before managing roles." /> : !manage ? <Alert variant="warning" title="Roles unavailable" message="The platform.roles.manage ability is required." /> : <RolesView canManage={manage} />}</>; }
