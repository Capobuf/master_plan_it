import { useApplicationContext } from "../../context/ApplicationContext"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import TenantsView from "../../components/tenants/TenantsView";
export default function TenantsHome() { const { loading, hasAbility } = useApplicationContext(); const view = hasAbility("platform.tenants.view"); return <><PageMeta title="Tenant | Master Plan IT" description="Registro Tenant di piattaforma" />{loading ? <Alert variant="info" title="Caricamento del contesto" message="Verifica delle autorizzazioni di piattaforma in corso." /> : !view ? <Alert variant="warning" title="Tenant non disponibili" message="Non disponi dell'autorizzazione necessaria per visualizzare questa pagina." /> : <TenantsView canView={view} canCreate={hasAbility("platform.tenants.create")} canUpdate={hasAbility("platform.tenants.update")} canDeactivate={hasAbility("platform.tenants.deactivate")} canReactivate={hasAbility("platform.tenants.reactivate")} />}</>; }
