import { useApplicationContext } from "../../context/ApplicationContext"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import UsersView from "../../components/users/UsersView";
export default function UsersHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const manage = hasAbility("platform.users.manage"); return <><PageMeta title="Utenti | Master Plan IT" description="Utenti e ruoli del Tenant" />{loading ? <Alert variant="info" title="Caricamento del contesto" message="Verifica del Tenant in corso." /> : tenant === null ? <Alert variant="warning" title="Tenant richiesto" message="Seleziona un Tenant prima di gestire gli utenti." /> : !manage ? <Alert variant="warning" title="Utenti non disponibili" message="Non disponi dell'autorizzazione necessaria per visualizzare questa pagina." /> : <UsersView canManage={manage} />}</>; }
