import { useApplicationContext } from "../../context/ApplicationContext"; import PageBreadcrumb from "../../components/common/PageBreadCrumb"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import UsersView from "../../components/users/UsersView";
export default function UsersHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const manage = hasAbility("platform.users.manage"); return <><PageMeta title="Users | Master Plan IT" description="Tenant users and roles" /><PageBreadcrumb pageTitle="Users" />{loading ? <Alert variant="info" title="Loading application context" message="Checking tenant and user access." /> : tenant === null ? <Alert variant="warning" title="Tenant required" message="Enter a tenant before managing users." /> : !manage ? <Alert variant="warning" title="Users unavailable" message="The platform.users.manage ability is required." /> : <UsersView canManage={manage} />}</>; }
