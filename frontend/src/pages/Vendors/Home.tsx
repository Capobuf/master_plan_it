import { useApplicationContext } from "../../context/ApplicationContext";
import PageBreadcrumb from "../../components/common/PageBreadCrumb"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import VendorsView from "../../components/vendors/VendorsView";
export default function VendorsHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const canView = hasAbility("vendor.view"); return <><PageMeta title="Vendors | Master Plan IT" description="Tenant vendor register" /><PageBreadcrumb pageTitle="Vendors" />{loading ? <Alert variant="info" title="Loading application context" message="Checking tenant and vendor access." /> : tenant === null ? <Alert variant="warning" title="Tenant required" message="Enter a tenant before managing vendors." /> : !canView ? <Alert variant="warning" title="Vendors unavailable" message="The vendor.view ability is required." /> : <VendorsView canView={canView} canCreate={hasAbility("vendor.create")} canUpdate={hasAbility("vendor.update")} canDeactivate={hasAbility("vendor.deactivate")} canReactivate={hasAbility("vendor.reactivate")} canDelete={hasAbility("vendor.delete")} canHistory={hasAbility("vendor.view-revisions")} canRestore={hasAbility("vendor.restore-revision")} />}</>; }
