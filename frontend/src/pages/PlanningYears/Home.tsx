import { useApplicationContext } from "../../context/ApplicationContext"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import PlanningYearsView from "../../components/planningYears/PlanningYearsView";
export default function PlanningYearsHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const view = hasAbility("planning-year.view"); return <><PageMeta title="Anni di Pianificazione | Master Plan IT" description="Anni di pianificazione del Tenant" />{loading ? <Alert variant="info" title="Caricamento del contesto" message="Verifica del Tenant in corso." /> : tenant === null ? <Alert variant="warning" title="Tenant richiesto" message="Seleziona un Tenant prima di gestire gli anni di pianificazione." /> : !view ? <Alert variant="warning" title="Anni non disponibili" message="Non disponi dell'autorizzazione necessaria per visualizzare questa pagina." /> : <PlanningYearsView canView={view} canCreate={hasAbility("planning-year.create")} canDeactivate={hasAbility("planning-year.deactivate")} canReactivate={hasAbility("planning-year.reactivate")} />}</>; }
