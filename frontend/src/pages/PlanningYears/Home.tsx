import { useApplicationContext } from "../../context/ApplicationContext"; import PageBreadcrumb from "../../components/common/PageBreadCrumb"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import PlanningYearsView from "../../components/planningYears/PlanningYearsView";
export default function PlanningYearsHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const view = hasAbility("planning-year.view"); return <><PageMeta title="Planning years | Master Plan IT" description="Tenant planning years" /><PageBreadcrumb pageTitle="Planning years" />{loading ? <Alert variant="info" title="Loading application context" message="Checking tenant and planning-year access." /> : tenant === null ? <Alert variant="warning" title="Tenant required" message="Enter a tenant before managing planning years." /> : !view ? <Alert variant="warning" title="Planning years unavailable" message="The planning-year.view ability is required." /> : <PlanningYearsView canView={view} canCreate={hasAbility("planning-year.create")} canDeactivate={hasAbility("planning-year.deactivate")} canReactivate={hasAbility("planning-year.reactivate")} />}</>; }
