import { useApplicationContext } from "../../context/ApplicationContext"; import PageBreadcrumb from "../../components/common/PageBreadCrumb"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import CostCentersView from "../../components/costCenters/CostCentersView";
export default function CostCentersHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const view = hasAbility("cost-center.view"); return <><PageMeta title="Cost centers | Master Plan IT" description="Tenant cost-center hierarchy" /><PageBreadcrumb pageTitle="Cost centers" />{loading ? <Alert variant="info" title="Loading application context" message="Checking tenant and cost-center access." /> : tenant === null ? <Alert variant="warning" title="Tenant required" message="Enter a tenant before managing cost centers." /> : !view ? <Alert variant="warning" title="Cost centers unavailable" message="The cost-center.view ability is required." /> : <CostCentersView canView={view} canCreate={hasAbility("cost-center.create")} canUpdate={hasAbility("cost-center.update")} canDeactivate={hasAbility("cost-center.deactivate")} canReactivate={hasAbility("cost-center.reactivate")} canDelete={hasAbility("cost-center.delete")} canHistory={hasAbility("cost-center.view-revisions")} canRestore={hasAbility("cost-center.restore-revision")} />}</>; }
