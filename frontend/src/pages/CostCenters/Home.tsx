import { useApplicationContext } from "../../context/ApplicationContext"; import PageMeta from "../../components/common/PageMeta"; import Alert from "../../components/ui/alert/Alert"; import CostCentersView from "../../components/costCenters/CostCentersView";
export default function CostCentersHome() { const { data, loading, hasAbility } = useApplicationContext(); const tenant = data?.tenant?.id ?? null; const view = hasAbility("cost-center.view"); return <><PageMeta title="Centri di Costo | Master Plan IT" description="Gerarchia dei centri di costo del Tenant" />{loading ? <Alert variant="info" title="Caricamento del contesto" message="Verifica del Tenant in corso." /> : tenant === null ? <Alert variant="warning" title="Tenant richiesto" message="Seleziona un Tenant prima di gestire i centri di costo." /> : !view ? <Alert variant="warning" title="Centri di costo non disponibili" message="Non disponi dell'autorizzazione necessaria per visualizzare questa pagina." /> : <CostCentersView canView={view} canCreate={hasAbility("cost-center.create")} canUpdate={hasAbility("cost-center.update")} canDeactivate={hasAbility("cost-center.deactivate")} canReactivate={hasAbility("cost-center.reactivate")} canDelete={hasAbility("cost-center.delete")} canHistory={hasAbility("cost-center.view-revisions")} canRestore={hasAbility("cost-center.restore-revision")} />}</>; }
