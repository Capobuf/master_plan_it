import Alert from "../../components/ui/alert/Alert";import TenantGeneralSettingsForm from "../../components/settings/TenantGeneralSettingsForm";import {useApplicationContext} from "../../context/ApplicationContext";
export default function GeneralSettings(){const{loading,hasAbility}=useApplicationContext();if(loading)return <Alert variant="info" title="Caricamento" message="Verifica delle autorizzazioni in corso."/>;const canUpdate=hasAbility("tenant-settings.update");if(!hasAbility("tenant-settings.view")&&!canUpdate)return <Alert variant="warning" title="Generali non disponibili" message="Non disponi dell'autorizzazione necessaria."/>;return <TenantGeneralSettingsForm canUpdate={canUpdate}/>;}
