@extends('layouts.app')
@section('title','Contratti')
@section('content')
<div class="mb-6 flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-gray-500">Operatività</p><h1 class="text-2xl font-semibold text-gray-800 dark:text-white">Contratti</h1><p class="mt-1 text-sm text-gray-500">Termini contrattuali e occorrenze di spesa materializzate.</p></div>@if($abilities['create'])<a href="{{ route('operational.contracts.create') }}" class="rounded-lg bg-brand-500 px-4 py-3 text-sm font-medium text-white">Nuovo contratto</a>@endif</div>
@if(count($contracts['data']))<div class="overflow-x-auto rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[0.03]"><table class="min-w-full text-left text-sm"><thead class="border-b border-gray-100 text-xs uppercase text-gray-500 dark:border-gray-800"><tr><th class="px-5 py-4">Contratto</th><th class="px-5 py-4">Fornitore</th><th class="px-5 py-4">Centro di costo</th><th class="px-5 py-4">Periodo</th><th class="px-5 py-4">Termini</th><th class="px-5 py-4">Generate</th><th class="px-5 py-4">Stato</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@foreach($contracts['data'] as $contract)<tr><td class="px-5 py-4"><a href="{{ route('operational.contracts.show',$contract['id']) }}" class="font-medium text-gray-800 hover:text-brand-500 dark:text-white">{{ $contract['title'] }}</a></td><td class="px-5 py-4">{{ $contract['vendorName'] ?? '—' }}</td><td class="px-5 py-4">{{ $contract['costCenterName'] ?? '—' }}</td><td class="px-5 py-4">{{ $contract['effectiveStart'] ?? '—' }} – {{ $contract['effectiveEnd'] ?? '—' }}</td><td class="px-5 py-4">{{ $contract['termCount'] }}</td><td class="px-5 py-4">{{ $contract['generatedExpenseCount'] }}</td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs {{ $contract['active'] ? 'bg-success-50 text-success-600' : 'bg-gray-100 text-gray-600' }}">{{ $contract['active'] ? 'Attivo' : 'Inattivo' }}</span></td></tr>@endforeach</tbody></table></div>@if($contracts['lastPage']>1)<nav class="mt-5 flex gap-1">@foreach($contracts['links'] as $link)<a class="rounded-lg px-3 py-2 text-sm {{ $link['active'] ? 'bg-brand-500 text-white' : 'text-gray-600 dark:text-gray-300' }}" href="{{ $link['url'] ?? '#' }}">{!! $link['label'] !!}</a>@endforeach</nav>@endif @else<div class="rounded-2xl border border-dashed border-gray-300 p-10 text-center text-sm text-gray-500 dark:border-gray-700">Non sono presenti contratti.</div>@endif
@endsection
