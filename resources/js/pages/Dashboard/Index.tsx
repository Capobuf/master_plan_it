import { Head, Link, usePage } from '@inertiajs/react';
import AppLayout from '../../layouts/AppLayout';
import { EmptyState, PageHeader } from '../../components/ui';
import type { SharedPageProps } from '../../types';
export default function Dashboard() { const { navigation, tenant } = usePage<SharedPageProps>().props; const modules = [{ label: 'Planning years', href: '/operational/planning-years', show: navigation.canViewPlanningYears }, { label: 'Cost centers', href: '/operational/cost-centers', show: navigation.canViewCostCenters }, { label: 'Vendors', href: '/operational/vendors', show: navigation.canViewVendors }, { label: 'Expenses', href: '/operational/expenses', show: navigation.canViewExpenses }].filter((m) => m.show); return <AppLayout><Head title="Dashboard"/><PageHeader title="Dashboard" description={tenant.current ? `Workspace for ${tenant.current.name}.` : 'Select a tenant to access operational modules.'} crumbs={[{ label: 'Dashboard' }]}/>{modules.length ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{modules.map((m) => <Link key={m.href} href={m.href} className="mp-card p-5 transition hover:-translate-y-0.5 hover:border-brand-300 hover:shadow-md"><h2 className="font-semibold text-slate-900">{m.label}</h2><p className="mt-2 text-sm text-slate-500">Open {m.label.toLowerCase()}.</p><span className="mt-5 inline-block text-sm font-semibold text-brand-700">View module →</span></Link>)}</div> : <EmptyState title="No operational modules available">Your account does not currently have access to an operational module.</EmptyState>}</AppLayout>; }
