import { useForm } from '@inertiajs/react'; import AppLayout from '../../../layouts/AppLayout'; import { Button, PageHeader, TextInput } from '../../../components/ui';
export function RoleForm({ role, abilities = [], method }: any) { const form = useForm<any>({ name: role?.name ?? '', abilities: role?.abilities ?? [], lock_version: role?.lockVersion ?? 0 }); const save = () => method === 'post' ? form.post('/operational/roles') : form.put(`/operational/roles/${role.id}`); return <AppLayout><PageHeader title={method === 'post' ? 'Add role' : 'Edit role'} crumbs={[{ label: 'Roles', href: '/operational/roles' }, { label: method === 'post' ? 'Create' : 'Edit' }]}/><form className="mp-card max-w-3xl" onSubmit={(e) => { e.preventDefault(); save(); }}><div className="mp-card-body"><TextInput label="Role name" required value={form.data.name} onChange={(e) => form.setData('name', e.target.value)} error={form.errors.name}/><fieldset className="mt-6"><legend className="mb-1.5 block text-sm font-medium text-gray-700 dark:text-gray-400">Abilities</legend><div className="grid gap-3 sm:grid-cols-2">{abilities.map((a: any) => { const value = typeof a === 'string' ? a : a.value; const label = typeof a === 'string' ? a : a.label; return <label key={value} className="flex gap-3 rounded-lg border border-slate-200 p-3 text-sm"><input type="checkbox" checked={form.data.abilities.includes(value)} onChange={(e) => form.setData('abilities', e.target.checked ? [...form.data.abilities, value] : form.data.abilities.filter((x: string) => x !== value))}/>{label}</label>; })}</div></fieldset><div className="mt-6"><Button disabled={form.processing}>{method === 'post' ? 'Create role' : 'Save changes'}</Button></div></div></form></AppLayout>; }
