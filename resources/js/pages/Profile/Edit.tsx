import { Head, useForm } from '@inertiajs/react';
import AppLayout from '../../layouts/AppLayout';
import { Button, PageHeader, TextInput } from '../../components/ui';
import type { ChangePasswordFormData } from '../../types/auth';
export default function ProfileEdit() { const form = useForm<ChangePasswordFormData>({ current_password: '', password: '', password_confirmation: '' }); return <AppLayout><Head title="Profile"/><PageHeader title="Profile" description="Manage your account security." crumbs={[{ label: 'Profile' }]}/><div className="mp-card max-w-2xl"><div className="mp-card-body"><h2 className="text-lg font-semibold text-slate-900">Change password</h2><form className="mt-6 space-y-5" onSubmit={(e) => { e.preventDefault(); form.put('/profile/password', { onSuccess: () => form.reset() }); }}><TextInput label="Current password" type="password" autoComplete="current-password" required value={form.data.current_password} onChange={(e) => form.setData('current_password', e.target.value)} error={form.errors.current_password}/><TextInput label="New password" type="password" autoComplete="new-password" required value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} error={form.errors.password}/><TextInput label="Confirm new password" type="password" autoComplete="new-password" required value={form.data.password_confirmation} onChange={(e) => form.setData('password_confirmation', e.target.value)} error={form.errors.password_confirmation}/><Button type="submit" disabled={form.processing}>Update password</Button></form></div></div></AppLayout>; }
