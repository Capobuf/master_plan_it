import { Head, useForm } from '@inertiajs/react';
import GuestLayout from '../../layouts/GuestLayout';
import { Button, TextInput } from '../../components/ui';
import type { LoginFormData } from '../../types/auth';
export default function Login() { const form = useForm<LoginFormData>({ email: '', password: '' }); return <GuestLayout><Head title="Sign in"/><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><div className="text-xl font-bold text-brand-950">Master Plan IT</div></div><div className="mp-card"><div className="mp-card-body"><h2 className="text-2xl font-bold text-slate-900">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to access your workspace.</p><form className="mt-7 space-y-5" onSubmit={(e) => { e.preventDefault(); form.post('/login'); }}><TextInput label="Email address" type="email" autoComplete="email" required value={form.data.email} onChange={(e) => form.setData('email', e.target.value)} error={form.errors.email}/><TextInput label="Password" type="password" autoComplete="current-password" required value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} error={form.errors.password}/><Button type="submit" className="w-full" disabled={form.processing}>Sign in</Button></form></div></div></div></GuestLayout>; }
