Microtask 1 — Aggiungere One Off a MPIT Actual Entry (senza rompere compatibilità)

File:

master_plan_it/doctype/mpit_actual_entry/mpit_actual_entry.json

master_plan_it/doctype/mpit_actual_entry/mpit_actual_entry.py

master_plan_it/doctype/mpit_actual_entry/mpit_actual_entry.js

translations/it.csv

report: master_plan_it/report/mpit_actual_entries/*

Azioni:

mpit_actual_entry.json

Aggiungere opzione Select One Off in entry_kind.

Rendere cost_center mandatory anche per One Off (usa mandatory_depends_on).

Rendere description mandatory per One Off (solo per One Off).

mpit_actual_entry.py

Estendere _enforce_entry_kind_rules():

accetta One Off

applica vincoli: no contract/project, cost_center obbligatorio, description obbligatoria

Aggiornare i messaggi di errore e aggiungere commenti in inglese sulle regole (solo dove non è ovvio).

mpit_actual_entry.js

Aggiornare sync_entry_kind_with_links():

se c’è contract o project ⇒ forza Delta anche se era One Off

se non ci sono link ⇒ non forzare automaticamente (lascia scelta utente)

report/mpit_actual_entries/*

Se il report filtra/conta per kind, includere One Off dove serve (e aggiornare etichette).

translations/it.csv

Aggiungere traduzioni per:

“One Off” → (es. “Una Tantum” oppure “One-Off”; scegli coerente con il resto delle traduzioni)

nuovi messaggi di validazione

Criteri di accettazione (funzionali):

Creo “One Off” con description+amount+cost_center, senza link: ok.

Se aggiungo un contract su una entry One Off, il kind va a Delta (UI) e server valida.

In overview/cap-vs-actual, una One Off Verified aumenta l’actual; una Draft no.

Microtask 2 — Sezione separata per One-Off (entrypoint UX, non rebranding)

Obiettivo: entrypoint dedicato “One-Off” senza introdurre un nuovo DocType.

Soluzione nativa e leggera:

Creare un Query Report dedicato che filtra entry_kind = One Off e aggiunge un bottone “New One-Off” che crea MPIT Actual Entry precompilando entry_kind.

File (nuovi):

master_plan_it/report/mpit_one_off_expenses/mpit_one_off_expenses.py

master_plan_it/report/mpit_one_off_expenses/mpit_one_off_expenses.js

master_plan_it/report/mpit_one_off_expenses/mpit_one_off_expenses.json

In mpit_one_off_expenses.js:

aggiungere bottone:

frappe.new_doc('MPIT Actual Entry', { entry_kind: 'One Off' })

Workspace:

master_plan_it/workspace/master_plan_it/master_plan_it.json

aggiungere un link/shortcut nella sezione che preferisci (“Spese” o nuova sezione “One-Off”)

Microtask 3 — Snapshot: rigenera senza perdere righe manuali consentite

File:

master_plan_it/doctype/mpit_budget/mpit_budget.py (funzione create_snapshot)

test: master_plan_it/doctype/mpit_budget/test_mpit_budget.py

Azioni:

In create_snapshot(year):

dopo aver copiato le righe generate dal Live:

trovare la snapshot più recente dello stesso anno (se esiste)

copiare solo righe manuali consentite (oggi: is_generated=0 e line_kind='Allowance' con manual_source_type='Allowance')

aggiungere commento in inglese sul perché si copiano solo quelle (coerenza filosofia snapshot + riduzione attrito).

Aggiornare/aggiungere test:

crea snapshot A con una manual allowance

crea snapshot B dallo stesso live

verifica che B contenga anche quella manual allowance

Microtask 4 — Budget workflow_state: impedire editing manuale del campo (evita i bug che hai visto)

File:

master_plan_it/doctype/mpit_budget/mpit_budget.json

opzionale: translations/it.csv per messaggi più chiari

Azioni:

Rendere workflow_state read-only (lascia allow_on_submit=1 se serve).

Server-side: mantenere invarianti già presenti (Live sempre Draft), ma l’utente non deve poter “impostare a mano” Proposed/Rejected/Approved sul Live.

Nota tecnica (documentata): i workflow in Frappe gestiscono docstatus e azioni; il campo stato non dovrebbe essere editato manualmente.

Microtask 5 — auto_renew impatta la generazione budget (solo per Years esistenti)

File:

master_plan_it/doctype/mpit_budget/mpit_budget.py (_generate_contract_lines, _generate_contract_term_lines)

budget_refresh_hooks.py + enqueue_budget_refresh() (filtro anni)

test: master_plan_it/doctype/mpit_budget/test_mpit_budget.py o test contratti

Azioni:

In query contratti per budget:

includere auto_renew nel select.

In _generate_contract_term_lines():

se term è l’ultimo e contract.auto_renew == 1 e non ci sono term futuri:

estendere term_end a year_end per l’anno del budget

se esiste un term che copre l’anno futuro, quello prevale (già succede perché il loop lo vede).

Vincolo “solo anni in MPIT Year”:

in enqueue_budget_refresh() e/o in budget_refresh_hooks.py:

filtrare years a quelli realmente presenti in MPIT Year

così non si auto-creano budget live “all’infinito” e quando crei un nuovo year la cosa funziona.

Aggiungere test:

contratto con term 2025 + auto_renew=1 ⇒ genera righe in budget 2026

aggiungo un term 2026 diverso ⇒ 2026 usa il term 2026, non l’estensione

Nota: in Frappe non è lecito riportare un documento submitted (docstatus=1) a draft (docstatus=0); quindi per snapshot “approved” la strada corretta è nuova versione, non “riapri e modifica”.