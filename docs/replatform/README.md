# Master Plan IT Laravel replatform documentation

This package is the documentation-only implementation contract for the Laravel replatform, anchored to the baseline in `baseline.md`.

Read in order: constitution → baseline/current state → source traceability → target architecture/ADRs → execution plan → feature `spec.md` → `plan.md` → contracts/data model → tasks → quickstart → convergence analysis.

The package contains no Laravel application implementation and makes no claim that tests or migrations were executed. `IMPLEMENTATION READY` applies only to documented scope; feature 006 remains not CUTOVER READY until real exports and hosting details are verified.
