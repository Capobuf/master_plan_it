# Tasks — Feature 003 Expense domain

Status: `PROPOSED TARGET — /speckit.tasks`  
Input: Constitution 3.0.1; current Feature 003 spec/plan/research/data model/contracts; shared tenant, permission, revision, attachment and error contracts.  
ID policy: former `T003-01`–`T003-22` are superseded because they encoded `ExpenseRowState`, replacement graphs, `ExpenseRowAudit` and `ReplaceExpenseRow`. New IDs use `T003-001` onward and no obsolete target symbol is retained.

Every task is test-first. Authoritative monetary assertions compare normalized decimal strings exactly.

## Phase 1 — Money and allocation foundation

- [ ] T003-001 Write pure accounting tests in `tests/Accounting/Unit/MoneyTest.php`, `VatCalculatorTest.php` and `MonthlyAllocatorTest.php`; symbols: decimal parser, scale/currency guards, add/subtract/multiply, half-up boundaries, included/excluded VAT, negative Actual, all/start/end allocation and residual; depends: Feature 001 T001-004; requirements: FR-003-040–FR-003-052, INV-AMT-001, INV-VAT-001, INV-DATE-001, INV-DIST-001; validate: `php artisan test tests/Accounting/Unit/MoneyTest.php tests/Accounting/Unit/VatCalculatorTest.php tests/Accounting/Unit/MonthlyAllocatorTest.php`; expected before implementation: focused failures from missing value objects; forbidden: float expected values, implementation-derived expectations or currency conversion.
- [ ] T003-002 Implement money primitives in `app/Domain/Money/Data/Money.php`, `VatBreakdown.php`, `app/Domain/Money/Services/MoneyCalculator.php`, `VatCalculator.php` and `MonthlyAllocator.php`; symbols: immutable decimal-string DTOs, BCMath operations, documented result rounding and deterministic residual assignment; depends: T003-001; requirements: FR-003-022, FR-003-040–FR-003-052; tests first: T003-001; validate: `php artisan test tests/Accounting/Unit/MoneyTest.php tests/Accounting/Unit/VatCalculatorTest.php tests/Accounting/Unit/MonthlyAllocatorTest.php`; expected: exact two-decimal business results and six-decimal intermediates with no float authority; forbidden: money package, locale-formatted internal values, exponent notation or silent scale truncation.

## Phase 2 — Expense and revision foundation

- [ ] T003-003 Write Expense schema tests in `tests/Feature/Expenses/ExpenseSchemaTest.php`; symbols: tenant-owned `expenses`/`expense_rows`, type/confirmation/source fields, decimal columns, XOR context, lock versions, soft deletion and unique source key; depends: Feature 002 T002-002, Feature 007 T007-012, T003-002; requirements: FR-003-001, FR-003-002, FR-003-010, FR-003-032, FR-003-061, FR-003-063, INV-EXP-002, INV-TEN-003; validate: `php artisan test tests/Feature/Expenses/ExpenseSchemaTest.php`; expected before implementation: focused failures; forbidden: current `state`, replacement link, persisted aggregate total, cross-tenant FK or cascade that bypasses Actions.
- [ ] T003-004 Create Expense persistence and typed input in `database/migrations/*_create_expenses_table.php`, `*_create_expense_rows_table.php`, `app/Models/Expense.php`, `ExpenseRow.php`, factories, `app/Domain/Expenses/Enums/ExpenseKind.php`, `ExpenseType.php`, `ActualConfirmationState.php`, `Distribution.php`, `app/Domain/Expenses/Data/SaveExpenseData.php` and `SaveExpenseRowData.php`; depends: T003-003; requirements: FR-003-001–FR-003-014, FR-003-032, FR-003-050, FR-003-051, FR-003-061, FR-003-063; tests first: T003-003; validate: `php artisan test tests/Feature/Expenses/ExpenseSchemaTest.php`; expected: explicit current model with no replacement graph and valid typed casts; forbidden: economic accessor/observer, global magic current scope or Eloquent models as public economic DTOs.
- [ ] T003-005 [P] Write versioning package smoke tests in `tests/Feature/Revisions/VersioningPackageSmokeTest.php`; symbols: Overtrue snapshot strategy, approved field allowlist, actor correlation, soft-deleted model history and disabled direct package restore; depends: Feature 001 T001-001, T003-004; requirements: FR-003-033–FR-003-035, INV-REV-001–INV-REV-003; validate: `php artisan test tests/Feature/Revisions/VersioningPackageSmokeTest.php`; expected before integration: package behavior is measured and direct `revert()` path is proven unavailable to UI; forbidden: DIFF strategy, secrets/attachment bytes in snapshots or treating package callbacks as domain authority.
- [ ] T003-006 Create revision persistence in `database/migrations/*_create_revision_batches_table.php`, `*_create_revision_batch_items_table.php`, `app/Models/RevisionBatch.php`, `RevisionBatchItem.php` and `app/Domain/Revisions/Data/RevisionOperation.php`; symbols: tenant/actor/root/operation/reason/correlation/restored-source and version links; depends: T003-005; requirements: FR-003-033, FR-003-034, FR-003-061, INV-REV-001, INV-TEN-003; tests first: create `tests/Feature/Revisions/RevisionBatchSchemaTest.php`; validate: `php artisan test tests/Feature/Revisions/RevisionBatchSchemaTest.php`; expected: one logical operation can link root and child snapshots without becoming current data; forbidden: generic event store, economic columns or revision cascade into current records.
- [ ] T003-007 Implement shared revision orchestration in `app/Domain/Revisions/Actions/BeginRevisionBatch.php`, `LinkVersionToRevisionBatch.php`, `app/Domain/Revisions/Queries/RevisionHistoryQuery.php` and `app/Policies/RevisionPolicy.php`; symbols: batch lifecycle inside owning transaction, tenant-safe compare DTO and no direct restore; depends: T003-006; requirements: FR-003-033, FR-003-034, FR-003-062, INV-REV-001, INV-TEN-003; tests first: create `tests/Feature/Revisions/RevisionBatchIntegrationTest.php`; validate: `php artisan test tests/Feature/Revisions/RevisionBatchIntegrationTest.php`; expected: changed aggregate models appear as one logical revision operation and remain excluded from current Queries; forbidden: opening independent transactions, package restore UI or a generic versioning framework beyond approved metadata.
- [ ] T003-008 Configure Expense permissions/policy in `app/Policies/ExpensePolicy.php` and `database/seeders/PermissionCatalogueSeeder.php`; symbols: view/create/update/delete/confirm/restore/history/attachment/output abilities; depends: T003-004, T003-007 and Feature 007 T007-009; requirements: FR-003-030, FR-003-062, INV-TEN-003; tests first: create `tests/Feature/Expenses/ExpenseAuthorizationTest.php`; validate: `php artisan test tests/Feature/Expenses/ExpenseAuthorizationTest.php`; expected: same-tenant permission matrix and safe other-tenant/inactive denial; forbidden: Editor/Viewer name conditions or Administrator invariant bypass.

**Checkpoint:** exact money, current persistence, revision batches and policy boundary exist before business stories.

## Phase 3 — US-003-01 Current expense register (P1, MVP)

**Goal:** show each current Expense once with exact server totals and no deleted/revision copies.

**Independent test:** fixture containing current, updated, restored and deleted data returns only current non-deleted rows with exact Net/VAT/Gross.

- [ ] T003-009 [P] [US1] Write register/query tests in `tests/Accounting/Integration/CurrentExpenseDatasetTest.php` and `tests/Feature/Expenses/ExpenseRegisterTest.php`; symbols: current/non-deleted source, tenant/year filters, no revision/audit/tombstone contribution, row/aggregate exact totals and pagination; depends: T003-008; requirements: FR-003-020–FR-003-022, FR-003-060, INV-EXP-001, INV-REV-001, INV-BAS-001; validate: `php artisan test tests/Accounting/Integration/CurrentExpenseDatasetTest.php tests/Feature/Expenses/ExpenseRegisterTest.php`; expected before implementation: focused failures; forbidden: SQL formulas that duplicate Money rules, loading full Eloquent graphs or reading version tables.
- [ ] T003-010 [US1] Implement register/detail Queries in `app/Domain/Expenses/Data/ExpenseRegisterRow.php`, `ExpenseDetail.php`, `app/Domain/Expenses/Queries/ExpenseRegisterQuery.php` and `ExpenseDetailQuery.php`; symbols: tenant-scoped scalar projection, exact row/aggregate DTOs, current-only predicates and attachment/revision summary references; depends: T003-009; requirements: FR-003-020–FR-003-022, FR-003-060, FR-003-061; tests first: T003-009; validate: `php artisan test tests/Accounting/Integration/CurrentExpenseDatasetTest.php tests/Feature/Expenses/ExpenseRegisterTest.php`; expected: deterministic current dataset with no hidden calculation in UI; forbidden: returning Eloquent models as report contract, caching totals or unbounded `with()` graphs.
- [ ] T003-011 [US1] Implement Filament register/detail in `app/Filament/Resources/Expenses/ExpenseResource.php`, Pages and `app/Filament/Resources/Expenses/Pages/ViewExpense.php`; symbols: current filters, server totals, active tenant context and links to files/history; depends: T003-010; requirements: FR-003-020–FR-003-022, FR-003-060–FR-003-062; tests first: create `tests/Livewire/Expenses/ExpenseRegisterResourceTest.php`; validate: `php artisan test tests/Livewire/Expenses/ExpenseRegisterResourceTest.php`; expected: each current expense appears once and direct other-tenant IDs deny safely; forbidden: presentation calculations, trashed records in ordinary resource or hidden permission checks.

**Checkpoint:** US1 is independently testable and is the Feature 003 MVP read slice.

## Phase 4 — US-003-02 Independent economic rows (P1)

**Goal:** create/update Expense aggregates containing independent Estimate, Quote and Actual rows with exact validation.

**Independent test:** each row type can be created directly; several distinct rows coexist; invalid funding, dates, VAT, context or stale row version rolls back all changes.

- [ ] T003-012 [P] [US2] Write aggregate tests in `tests/Feature/Expenses/CreateExpenseTest.php`, `UpdateExpenseTest.php` and `tests/Accounting/Integration/ExpenseCalculationTest.php`; symbols: full intended row set, explicit deletion marker only, no progression, quantity price, VAT, date/allocation, Extra/Plafond, project XOR contract, transaction rollback and revision batch; depends: T003-008; requirements: FR-003-001–FR-003-014, FR-003-030, FR-003-040–FR-003-052, FR-003-063, INV-EXP-002–INV-EXP-004, INV-PLF-001, INV-PLF-002; validate: `php artisan test tests/Feature/Expenses/CreateExpenseTest.php tests/Feature/Expenses/UpdateExpenseTest.php tests/Accounting/Integration/ExpenseCalculationTest.php`; expected before implementation: focused failures; forbidden: missing-row implicit deletion, mandatory Estimate→Quote→Actual, partial save or trusting browser-calculated money.
- [ ] T003-013 [US2] Implement aggregate Actions in `app/Domain/Expenses/Actions/CreateExpense.php`, `UpdateExpense.php` and shared validator `app/Domain/Expenses/Services/ExpenseAggregateValidator.php`; symbols: `execute(SaveExpenseData, User, TenantContext)`, explicit current row diff, row locks, Money calculation, revision batch, audit and typed result; depends: T003-012; requirements: FR-003-001–FR-003-014, FR-003-030, FR-003-040–FR-003-052, FR-003-063; tests first: T003-012; validate: `php artisan test tests/Feature/Expenses/CreateExpenseTest.php tests/Feature/Expenses/UpdateExpenseTest.php tests/Accounting/Integration/ExpenseCalculationTest.php`; expected: one transaction preserves aggregate/current invariants and exact values; forbidden: deadlock retry, observer writes, silent row removal, duplicate source key mutation or broad service layer.
- [ ] T003-014 [US2] Implement Filament editor in `app/Filament/Resources/Expenses/Pages/CreateExpense.php`, `EditExpense.php`, `app/Filament/Resources/Expenses/RelationManagers/ExpenseRowsRelationManager.php` and form schema classes; symbols: type-specific fields, row IDs/lock versions, explicit delete selection, server recalculation and error focus; depends: T003-013; requirements: FR-003-010–FR-003-014, FR-003-030, FR-003-040–FR-003-052; tests first: create `tests/Livewire/Expenses/ExpenseEditorTest.php`; validate: `php artisan test tests/Livewire/Expenses/ExpenseEditorTest.php`; expected: UI serializes intended current set and delegates one aggregate Action; forbidden: formulas in Livewire/JavaScript, implicit omission delete or custom SPA grid.

## Phase 5 — US-003-03 Actual confirmation (P1)

**Goal:** confirm Actual rows while preserving later correction/restore/delete permissions and stopping contract overwrite.

**Independent test:** only Actual `ToConfirm` confirms once, records actor/time, becomes user-authoritative and remains editable/deletable through authorized Actions.

- [ ] T003-015 [P] [US3] Write confirmation tests in `tests/Feature/Expenses/ConfirmActualTest.php` and `tests/Accounting/Integration/GeneratedActualOwnershipTest.php`; symbols: type/status guard, actor/time, system-managed transition, stale version, permission, idempotent already-confirmed response and later update/delete availability; depends: T003-013; requirements: FR-003-031, FR-003-032, FR-003-062, INV-ACT-001; validate: `php artisan test tests/Feature/Expenses/ConfirmActualTest.php tests/Accounting/Integration/GeneratedActualOwnershipTest.php`; expected before implementation: focused failures; forbidden: permanent immutability, source-key rewrite or contract-owned confirmation transaction.
- [ ] T003-016 [US3] Implement `ConfirmActual` in `app/Domain/Expenses/Actions/ConfirmActual.php` and Filament action in `app/Filament/Resources/Expenses/Actions/ConfirmActualAction.php`; symbols: exact permission, row/aggregate lock, status/actor/time, `is_system_managed=false`, revision batch and audit; depends: T003-015; requirements: FR-003-031, FR-003-032, FR-003-062; tests first: T003-015; validate: `php artisan test tests/Feature/Expenses/ConfirmActualTest.php tests/Accounting/Integration/GeneratedActualOwnershipTest.php`; expected: confirmation changes only approved fields and leaves version/update/delete paths enabled; forbidden: mutating contract, removing source identity or disabling later policy abilities.

## Phase 6 — US-003-04 Revision history (P2)

**Goal:** compare aggregate revisions and restore a valid snapshot as a new current revision.

**Independent test:** one aggregate save displays one revision batch; valid restore creates a new revision; invalid tenant/reference/source/money/current-state restore rolls back unchanged.

- [ ] T003-017 [P] [US4] Write revision/restore tests in `tests/Feature/Expenses/ExpenseRevisionHistoryTest.php` and `RestoreExpenseRevisionTest.php`; symbols: batch grouping, field diff, source snapshot, current authorization, full invariant revalidation, stale conflict and immutable prior history; depends: T003-007, T003-013; requirements: FR-003-033, FR-003-034, FR-003-062, INV-REV-002, INV-REV-003; validate: `php artisan test tests/Feature/Expenses/ExpenseRevisionHistoryTest.php tests/Feature/Expenses/RestoreExpenseRevisionTest.php`; expected before implementation: focused failures; forbidden: `version->revert()`, rewriting later versions or restoring inactive/new-selection references without approved current-value semantics.
- [ ] T003-018 [US4] Implement `RestoreExpenseRevision` and history UI in `app/Domain/Expenses/Actions/RestoreExpenseRevision.php`, `app/Domain/Expenses/Queries/ExpenseRevisionDetailQuery.php`, `app/Filament/Resources/Expenses/Pages/ExpenseRevisionHistory.php` and `CompareExpenseRevisions.php`; symbols: snapshot-to-typed-data mapper, current-rule Action reuse, restore source link and compare DTO; depends: T003-017; requirements: FR-003-033, FR-003-034, FR-003-062; tests first: T003-017; validate: `php artisan test tests/Feature/Expenses/ExpenseRevisionHistoryTest.php tests/Feature/Expenses/RestoreExpenseRevisionTest.php`; expected: restore is a new current operation and package storage never becomes current; forbidden: direct snapshot mass assignment, unscoped deleted-record history or presentation-side diff authority.

## Phase 7 — US-003-05 Delete expense or row (P2)

**Goal:** explicitly remove current economic records while retaining only minimized evidence and coherent files/history.

**Independent test:** authorized explicit deletion removes row/expense from every current query/output; deleting the last row requires deleting the Expense or adding a valid replacement in the same explicit transaction; failure restores DB/file state.

- [ ] T003-019 [P] [US5] Write deletion tests in `tests/Feature/Expenses/DeleteExpenseTest.php`, `DeleteExpenseRowTest.php` and `tests/Feature/Attachments/ExpenseAttachmentLifecycleTest.php`; symbols: explicit confirmation, aggregate last-row rule, soft-delete current exclusion, tombstone minimization, file finalize/cleanup, tenant/permission and rollback; depends: T003-013; requirements: FR-003-002, FR-003-030, FR-003-035, FR-003-061, FR-003-062, INV-EXP-001, INV-REV-001; validate: `php artisan test tests/Feature/Expenses/DeleteExpenseTest.php tests/Feature/Expenses/DeleteExpenseRowTest.php tests/Feature/Attachments/ExpenseAttachmentLifecycleTest.php`; expected before implementation: focused failures; forbidden: silent delete, retained payload bytes in audit, implicit row omission delete or current query including trashed rows.
- [ ] T003-020 [US5] Implement delete/file Actions in `app/Domain/Expenses/Actions/DeleteExpense.php`, `DeleteExpenseRow.php`, `app/Domain/Attachments/Actions/UploadAttachment.php`, `DeleteAttachment.php`, `app/Http/Controllers/Attachments/DownloadAttachmentController.php` and Expense Filament delete actions; symbols: explicit authorized intent, revision/tombstone, private file authorization and failure compensation; depends: T003-019; requirements: FR-003-030, FR-003-035, FR-003-061, FR-003-062; tests first: T003-019; validate: `php artisan test tests/Feature/Expenses/DeleteExpenseTest.php tests/Feature/Expenses/DeleteExpenseRowTest.php tests/Feature/Attachments/ExpenseAttachmentLifecycleTest.php`; expected: deleted records/files disappear from active paths and failures remain diagnostic; forbidden: public file URL, orphan cleanup suppression, hard delete of revision identity or asynchronous hidden file work.

## Phase 8 — Migration and complete verification

- [ ] T003-021 [P] Create deterministic legacy lifecycle fixtures in `tests/Fixtures/Migration/expense-lifecycle/` and mapping tests in `tests/Accounting/Integration/LegacyExpenseMappingTest.php`; symbols: accepted current row selection from legacy Active/Replaced/Cancelled evidence, initial snapshots, exact current-total reconciliation and mutable target Actual; depends: T003-013, T003-018; requirements: migration notes, FR-003-020, FR-003-031, INV-EXP-001, INV-REV-001; validate: `php artisan test tests/Accounting/Integration/LegacyExpenseMappingTest.php`; expected: one target current identity and unchanged accepted totals; forbidden: importing replacement states as current enum or inventing missing same-cost identity.
- [ ] T003-022 Run full Feature 003 verification and update `specs/003-expense-domain/quickstart.md`, `docs/replatform/source-traceability.md` and accounting fixtures with actual results; depends: T003-011, T003-014, T003-016, T003-018, T003-020, T003-021; requirements: FR-003-001–FR-003-063; validate: `composer test:accounting && php artisan test tests/Feature/Expenses tests/Feature/Revisions tests/Feature/Attachments && composer test:static`; expected: exact accounting, tenant, permission, revision and deletion suites pass; bounded Dusk runs only for row editor/focus/delete confirmation; forbidden: completion with float casts, skipped MySQL integration or stale replacement symbols.

## Dependencies and execution order

`T003-001 → T003-002 → T003-003 → T003-004`; version smoke/batches and permission setup follow. US1 and US2 tests may start after foundation; US3–US5 depend on aggregate Actions. Migration fixtures follow stable current/revision behavior.

## MVP scope

T003-001–T003-014 provide exact Money plus current Expense register and independent row create/update. Confirmation, history and deletion are subsequent independently testable increments.
