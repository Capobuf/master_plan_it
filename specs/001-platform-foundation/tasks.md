# Tasks — Feature 001 Platform foundation

Status: `REMEDIATED AFTER /speckit.analyze — REVIEW REQUIRED`  
Input: Constitution 3.0.1; Feature 001 spec/plan; development/test, permission and task-readiness contracts.  
Normative supplement: `../../docs/replatform/task-readiness-registry.md`.

Every task is test-first. `[FND]` identifies setup/foundational work, `[USn]` maps to a Feature 001 user story, and `[VER]` creates no new behavior. Feature 007 owns Tenant, `TenantContext`, tenant lifecycle and tenant User/Role/Tenant resources. Feature 001 owns application bootstrap, authentication integration, password operations, platform settings/audit, the notification delivery primitive, final scheduler registration and release workflows.

## Phase 1 — Setup

- [ ] T001-001 [FND] Create Laravel 13.22.0 skeleton and exact PHP dependency constraints in `composer.json`, `composer.lock`; symbols: PHP 8.3.32 platform, Sail 1.64.0, Filament 5.7.3, Livewire 4.3.3, Permission 8.3.0, Shield 4.3.1, Mansoor 5.1, Overtrue 6.0.0, OpenSpout 4.32.0; depends: none; requirements: FR-001-005, NFR-001-MAINT-01; tests first: `tests/Architecture/DependencyContractTest.php`; validate: `composer validate --strict && composer update --with-all-dependencies --no-interaction && php artisan about`; expected: exact lock resolves on PHP 8.3.32 with no unapproved package; forbidden: floating constraints, downgrade, `--ignore-platform-reqs`, Preline, Redis/worker or server-PDF dependency.
- [ ] T001-002 [P] [FND] Create frontend lock/build in `package.json`, `package-lock.json`, `vite.config.js`, `resources/js/app.js`; symbols: Vite manifest, Chart.js sole chart dependency, Filament entrypoints; depends: T001-001; requirements: FR-001-005, INV-PLT-003; tests first: extend `tests/Architecture/DependencyContractTest.php`; validate: `npm ci && npm run build && test -f public/build/manifest.json`; expected: deterministic assets and no production Node runtime; forbidden: CDN runtime, second UI/chart kit or client economic formulas.
- [ ] T001-003 [FND] Create canonical Sail environment in `compose.yaml`, `docker/8.3/Dockerfile`, `.env.example`, `.env.testing.example`; symbols: `laravel.test`, MySQL 8.4.10, optional selenium profile, separate `master_plan_it` and `master_plan_it_test`; depends: T001-001; requirements: FR-001-005, INV-PLT-004; tests first: `tests/Architecture/DevelopmentEnvironmentTest.php`; validate: `docker compose config && ./vendor/bin/sail up -d mysql laravel.test && ./vendor/bin/sail php -v && ./vendor/bin/sail mysql --version`; expected: reproducible runtime and persistent separate test DB; forbidden: floating tags, shared DB, Redis/worker or destructive initialization.
- [ ] T001-004 [FND] Configure `phpunit.xml`, Composer quality scripts, `phpstan.neon`, `pint.json`, `.github/CODEOWNERS`; symbols: `test:prepare`, `test:static`, `test:accounting`, `test:application`, `test:browser`, `verify`; depends: T001-001, T001-003; requirements: NFR-001-INT-01, NFR-001-LOG-01; tests first: `tests/Architecture/DevelopmentContractTest.php`; validate: `composer test:static`; expected: reset/test bypass and float authority are blocking; forbidden: `RefreshDatabase`, `DatabaseTruncation`, `migrate:fresh`, `db:wipe`, unscoped truncate or empty assertions.

## Phase 2 — Foundational platform persistence and integration

- [ ] T001-005 [FND] Create exact platform migrations/models listed in the readiness registry; symbols: singleton `PlatformSetting`, active `User`, append-only `AuditEvent`, database notification persistence and lock versions; depends: T001-003; requirements: FR-001-007, FR-001-017, FR-001-020, INV-PLT-006, INV-PLT-007; tests first: `tests/Feature/Platform/PlatformSchemaTest.php`; validate: `composer test:prepare && php artisan test tests/Feature/Platform/PlatformSchemaTest.php`; expected: forward migration and default retention 24; forbidden: generic JSON settings, password/token audit fields or per-tenant retention.
- [ ] T001-006 [FND] Configure package authorization in exact files from the readiness registry; symbols: teams `tenant_id`, protected global Administrator, stable catalogue and Editor/Viewer seeds; depends: T001-001, T001-005; requirements: FR-001-003, FR-001-008, FR-001-010, INV-PLT-005; tests first: `tests/Feature/Authorization/PermissionCatalogueTest.php`; validate: `php artisan test tests/Feature/Authorization/PermissionCatalogueTest.php`; expected: idempotent catalogue matches documentation and protected abilities cannot be tenant-assigned; forbidden: role-name business branches, direct-user permission UI, runtime generation or super-admin invariant bypass.
- [ ] T001-007 [FND] Integrate active-user and Feature 007 context middleware in `app/Http/Middleware/EnsureActiveUser.php`, `bootstrap/app.php`, `app/Providers/AppServiceProvider.php`; symbols: request-scoped `TenantContext`, middleware aliases/order and active-user deny; depends: T001-005, T001-006, T007-003; requirements: FR-001-002, FR-001-009, FR-001-012, INV-PLT-001, INV-TEN-001; tests first: `tests/Feature/Tenancy/TenantContextMiddlewareTest.php`; validate: `php artisan test tests/Feature/Tenancy/TenantContextMiddlewareTest.php`; expected: one context implementation and inactive-user denial; forbidden: duplicate context, unscoped fallback, impersonation or invalid session reuse.

## Phase 3 — US-001-01 Authenticate (P1, MVP)

**Independent test:** active Administrator reaches global shell; active tenant user reaches own tenant; invalid credentials, deactivated user and inactive tenant deny.

- [ ] T001-008 [US1] Write `tests/Feature/Auth/AuthenticationTest.php`, `tests/Feature/Auth/NoPasswordRecoveryRouteTest.php`; symbols: success/failure, active-state/tenant denial and absent public recovery routes; depends: T001-007; requirements: FR-001-001, FR-001-002, FR-001-012, FR-001-014, INV-PLT-001; validate: `php artisan test tests/Feature/Auth/AuthenticationTest.php tests/Feature/Auth/NoPasswordRecoveryRouteTest.php`; expected before implementation: focused failures; forbidden: mail self-reset, public registration or navigation-only authorization assertion.
- [ ] T001-009 [US1] Configure Filament auth/panel in `app/Providers/Filament/AdminPanelProvider.php`, `config/auth.php`, `app/Models/User.php`; symbols: `canAccessPanel()`, local email/password, session regeneration/logout; depends: T001-008; requirements: FR-001-001, FR-001-002, FR-001-014; tests first: T001-008; validate: same command as T001-008; expected: authentication grants no business ability by itself; forbidden: social login, magic link or middleware bypass.

## Phase 4 — US-001-02 Permission-aware shell (P1)

**Independent test:** granted navigation appears, direct unauthorized route denies, and tenant context remains visible.

- [ ] T001-010 [P] [US2] Write `tests/Feature/Authorization/NavigationPolicyTest.php`, `tests/Livewire/Shell/TenantContextIndicatorTest.php`; symbols: permission navigation, direct-route deny and visible Feature 007 indicator/breadcrumb; depends: T001-009, T007-006; requirements: FR-001-004, FR-001-009, INV-PLT-002; validate: `php artisan test tests/Feature/Authorization/NavigationPolicyTest.php tests/Livewire/Shell/TenantContextIndicatorTest.php`; expected before implementation: focused failures; forbidden: role-name assertions or hidden navigation treated as authorization.
- [ ] T001-011 [US2] Integrate shell hooks in `app/Providers/Filament/AdminPanelProvider.php`, `app/Providers/AuthServiceProvider.php`, `resources/views/filament/components/global-context.blade.php`; symbols: policy navigation, global/tenant shell distinction and Feature 007 indicator rendering; depends: T001-010; requirements: FR-001-004, FR-001-007, FR-001-009; tests first: T001-010; validate: same command as T001-010; expected: one indicator implementation; forbidden: duplicate component, Preline, SPA shell or client-only authorization.

## Phase 5 — US-001-03 Administrator identity/role integration (P1)

**Independent test:** Feature 007 resources are Administrator-only, protected abilities remain hidden/denied, and password reset is secret-safe.

- [ ] T001-012 [P] [US3] Write `tests/Feature/IdentityAccess/PlatformIdentityIntegrationTest.php`, `tests/Feature/IdentityAccess/PasswordAdministrationTest.php`; symbols: resource registration/access, protected ability deny, reset/session invalidation and secret minimization; depends: T001-006, T007-009, T007-010; requirements: FR-001-003, FR-001-008, FR-001-010, FR-001-013, FR-001-015, FR-001-017, INV-PLT-005, INV-PLT-006; validate: focused identity tests; expected before integration: focused failures; forbidden: duplicate lifecycle Actions, password value assertions or cross-tenant reset.
- [ ] T001-013 [US3] Implement `app/Domain/IdentityAccess/Actions/ResetTenantUserPassword.php`, `app/Domain/IdentityAccess/Actions/InvalidateUserSessions.php`; symbols: Administrator authorization, same-tenant target, one hash, session invalidation and minimized audit; depends: T001-012; requirements: FR-001-008, FR-001-015, FR-001-017; tests first: T001-012; validate: `php artisan test tests/Feature/IdentityAccess/PasswordAdministrationTest.php`; expected: transactional secret-safe reset; forbidden: user lifecycle mutation, plaintext logging or platform-role mutation.
- [ ] T001-014 [US3] Integrate Feature 007 resources and reset Action in `app/Providers/Filament/AdminPanelProvider.php`, `app/Filament/Resources/Users/Actions/ResetTenantUserPasswordAction.php`; symbols: Administrator navigation and Action delegation; depends: T001-013, T007-010; requirements: FR-001-003, FR-001-008, FR-001-015; tests first: T001-012; validate: identity integration tests; expected: one User/Role resource implementation; forbidden: duplicate resources or direct model save.

## Phase 6 — US-001-04 User changes own password (P2)

- [ ] T001-015 [P] [US4] Write `tests/Feature/IdentityAccess/ChangeOwnPasswordTest.php`, `tests/Feature/Console/ResetAdministratorPasswordCommandTest.php`; symbols: current-password, hidden input, hashing, session invalidation, no secret audit; depends: T001-013; requirements: FR-001-014–FR-001-017, INV-PLT-006; validate: focused password tests; expected before implementation: focused failures; forbidden: reset token/email or plaintext console capture.
- [ ] T001-016 [US4] Implement `app/Domain/IdentityAccess/Actions/ChangeOwnPassword.php`, `app/Console/Commands/ResetAdministratorPasswordCommand.php`, `app/Filament/Pages/Profile.php`; symbols: authenticated self-change and interactive emergency reset; depends: T001-015; requirements: FR-001-015–FR-001-017; tests first: T001-015; validate: same tests; expected: session invalidation without public recovery; forbidden: default password or secret side-channel.

## Phase 7 — US-001-05 Notification delivery primitive (P2)

- [ ] T001-017 [P] [US5] Write `tests/Feature/Notifications/NotificationDeduplicationTest.php`, `tests/Feature/Notifications/SynchronousMailFailureTest.php`; symbols: recipient/event dedup, database-first persistence and explicit mail result; depends: T001-005, T001-006; requirements: FR-001-018, INV-PLT-004; validate: `php artisan test tests/Feature/Notifications/NotificationDeduplicationTest.php tests/Feature/Notifications/SynchronousMailFailureTest.php`; expected before implementation: focused failures; forbidden: `ShouldQueue`, retry loop, Redis/WebSocket or swallowed mail failure.
- [ ] T001-018 [US5] Implement `app/Domain/Notifications/Actions/DeliverSynchronousNotification.php`, `app/Domain/Notifications/Data/NotificationOccurrence.php`, `app/Models/DatabaseNotification.php`; symbols: deterministic dedup and explicit database/mail result; depends: T001-017 only; requirements: FR-001-018; tests first: T001-017; validate: same tests; expected: reusable delivery primitive independent of feature commands and scheduler registration; forbidden: `routes/console.php` registration, business-specific queries, hidden retry or secret payload.

## Phase 8 — US-001-06 Platform settings and retention (P2)

- [ ] T001-019 [P] [US6] Write `tests/Feature/Platform/PlatformSettingTest.php`, `tests/Feature/Audit/AuditRetentionTest.php`, `tests/Livewire/Platform/PlatformSettingPageTest.php`; symbols: default 24, authorization, stale lock, reinforced lowering and bounded prune; depends: T001-005, T001-007; requirements: FR-001-019–FR-001-021, INV-PLT-007; validate: focused platform/audit tests; expected before implementation: focused failures; forbidden: per-tenant retention, implicit immediate prune or protected-table deletion.
- [ ] T001-020 [US6] Implement `app/Domain/Platform/Actions/UpdatePlatformSettings.php`, `app/Domain/Audit/Actions/PruneExpiredAuditEvents.php`, `app/Console/Commands/AuditPruneCommand.php`, `app/Policies/PlatformSettingPolicy.php`; symbols: singleton lock, current cutoff, ID batches and stable errors; depends: T001-019; requirements: FR-001-019–FR-001-021; tests first: T001-019; validate: focused platform/audit tests; expected: explicit update and prune; forbidden: observer prune, `expires_at`, retry or generic settings service.
- [ ] T001-021 [US6] Implement `app/Filament/Pages/PlatformSettings.php`, `resources/views/filament/pages/platform-settings.blade.php`; symbols: typed form, lock and lowering confirmation; depends: T001-020; requirements: FR-001-019, FR-001-020; tests first: T001-019; validate: platform settings Livewire test; expected: save/prune separation; forbidden: generic key editor or tenant-role access.

## Phase 9 — Cross-feature verification surfaces

- [ ] T001-022 [P] [VER] Create `.github/workflows/quality.yml`; symbols: static/accounting/application/browser/MySQL 8.4 jobs and lock-keyed caches; depends: T001-004; requirements: FR-001-005, INV-PLT-003; tests first: `tests/Architecture/WorkflowContractTest.php`; validate: workflow contract test; expected: mandatory gates on PR/protected branch; forbidden: optional mandatory jobs, unpinned actions or destructive DB reset.
- [ ] T001-023 [VER] Create `.github/workflows/release.yml`, `scripts/build-release.sh`, `tests/Feature/Deployment/ReleaseArtifactContractTest.php`; symbols: verified commit, production dependencies/assets, ZIP/checksum/provenance; depends: T001-002, T001-022; requirements: FR-001-005, INV-PLT-003; tests first: release contract test; validate: release contract plus `bash scripts/build-release.sh --verify-only`; expected: immutable host-ready ZIP; forbidden: production rebuild, `.env`, test data, `node_modules` or mutable latest artifact.
- [ ] T001-025 [VER] Write `tests/Feature/Scheduler/SchedulerRegistrationTest.php` and register `contracts:check-renewals`, operation-failure checks and `audit:prune` in `routes/console.php`; symbols: one cron-compatible schedule, overlap locks and explicit command ownership; depends: T001-018, T004-020, T006-014, T001-020; requirements: FR-001-006, FR-001-018, INV-PLT-004; validate: `php artisan schedule:list && php artisan test tests/Feature/Scheduler/SchedulerRegistrationTest.php`; expected: scheduler registration occurs only after feature commands exist; forbidden: business logic in scheduler closures, circular dependency or permanent worker.
- [ ] T001-026 [P] [VER] Write `tests/Feature/Audit/AuditViewAuthorizationTest.php`, `tests/Feature/Audit/AuditViewMinimizationTest.php`, `tests/Livewire/Audit/AuditLogPageTest.php`; symbols: same-tenant `audit.view`, Administrator tenant/global scope, minimized properties and no export route; depends: T001-005, T007-012; requirements: FR-001-007, FR-001-017, Q-020, INV-PLT-006, INV-TEN-006; validate: focused audit-view tests; expected before implementation: focused failures; forbidden: audit export, secret/file payload or cross-tenant existence leak.
- [ ] T001-027 [VER] Implement `app/Domain/Audit/Queries/AuditEventListQuery.php`, `app/Policies/AuditEventPolicy.php`, `app/Filament/Pages/AuditLogPage.php`, `resources/views/filament/pages/audit-log-page.blade.php`; symbols: tenant/global filters, bounded pagination, minimized DTO and no download action; depends: T001-026; requirements: Q-020; tests first: T001-026; validate: focused audit-view tests; expected: permission-controlled tenant/global audit view with no export; forbidden: raw payload rendering, Eloquent model exposure or export endpoint.
- [ ] T001-024 [VER] Run Feature 001 verification and update quickstart/traceability with actual results; depends: T001-009, T001-011, T001-014, T001-016, T001-018, T001-021, T001-023, T001-025, T001-027; requirements: FR-001-001–FR-001-021; validate: `composer verify`; expected: mandatory suites and bounded Dusk pass; forbidden: skipped checks, unexecuted claims or weakened tests.

## Dependencies and execution order

`T001-001 → {T001-002,T001-003} → T001-004 → {T001-005,T001-006} → T007-003 → T001-007 → US1 → US2/US3 → US4/US5/US6 → feature commands T004-020/T006-014 → T001-025 → audit/release verification → T001-024`.

## MVP scope

T001-001–T001-009 plus Feature 007 context foundation provide reproducible scaffold, safe tests and authentication. No fake single-tenant fallback is permitted.