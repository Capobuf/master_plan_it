# Tasks — Feature 001 Platform foundation

Status: `IMPLEMENTATION IN PROGRESS`
Input: Constitution 5.0.0; Feature 001 spec/plan; development/test, permission and task-readiness contracts.
Normative supplements: `../../docs/replatform/task-readiness-registry.md` and `../../docs/replatform/task-execution-registry.md`.

Every task is test-first. `[FND]` identifies setup/shared infrastructure, `[USn]` maps to a Feature 001 user story, and `[VER]` creates no runtime behavior. Feature 007 owns Tenant, `TenantContext`, tenant lifecycle and tenant User/Role/Tenant resources. Feature 001 owns application bootstrap, authentication integration, password operations, platform settings/audit, the notification delivery primitive, final scheduler registration and release workflows.

Foundation-story ownership: T001-001–T001-004 and T001-007 benefit US-001-01; T001-005 benefits US-001-06; T001-006 benefits US-001-03; T001-022–T001-023 benefit US-001-01.

## Phase 1 — Setup

- [X] T001-001 [FND] Create Laravel 13.22.0 skeleton and exact PHP dependency constraints in `composer.json`, `composer.lock`; symbols: PHP 8.3.32 platform, required BCMath extension, Sail 1.64.0, Filament 5.7.3, Livewire 4.3.3, Permission 8.3.0, Shield 4.3.1, Mansoor 5.1, Overtrue 6.0.0, OpenSpout 4.32.0; depends: none; requirements: FR-001-005, NFR-001-MAINT-01, Constitution C-02; tests first: `tests/Architecture/DependencyContractTest.php`; validate: `composer validate --strict --no-check-all && composer update --with-all-dependencies --no-interaction && php artisan about`; expected: schema/lock validation stays strict while Composer's generic exact-constraint advice is disabled because exact direct pins are mandatory; exact direct allowlist and lock resolve on PHP 8.3.32 with BCMath and no unapproved package; forbidden: floating package constraints, undeclared direct dependencies, downgrade, `--ignore-platform-reqs`, Preline, Redis/worker or server-PDF dependency.
- [X] T001-002 [P] [FND] Create frontend lock/build in `package.json`, `package-lock.json`, `vite.config.js`, `resources/js/app.js`; symbols: Vite manifest, Chart.js sole chart dependency, Filament entrypoints; depends: T001-001; requirements: FR-001-005, INV-PLT-003; tests first: extend `tests/Architecture/DependencyContractTest.php`; validate: `npm ci && npm run build && test -f public/build/manifest.json`; expected: deterministic assets and no production Node runtime; forbidden: CDN runtime, second UI/chart kit or client economic formulas.
- [X] T001-003 [FND] Create canonical Sail environment in `compose.yaml`, `docker/8.3/Dockerfile`, `.env.example`, `.env.testing.example`; symbols: `laravel.test`, MySQL 8.4.10, optional selenium profile, separate `master_plan_it` and `master_plan_it_test`; depends: T001-001; requirements: FR-001-005, INV-PLT-004; tests first: `tests/Architecture/DevelopmentEnvironmentTest.php`; validate: `docker compose config && ./vendor/bin/sail up -d mysql laravel.test && ./vendor/bin/sail php -v && ./vendor/bin/sail mysql --version`; expected: reproducible runtime and persistent separate test DB; forbidden: floating tags, shared DB, Redis/worker or destructive initialization.
- [X] T001-004 [FND] Configure `phpunit.xml`, `composer.json` quality scripts, `tests/DuskTestCase.php`, `tests/Browser/Support/BrowserMatrix.php`, `tests/Architecture/WriteRollbackCoverageTest.php`, `tests/Architecture/Fixtures/domain-write-rollback-map.php`, `phpstan.neon`, `pint.json`, `.github/CODEOWNERS`; symbols: `test:prepare`, `test:static`, `test:accounting`, `test:application`, `test:browser`, `test:browser-matrix`, an explicit write-to-rollback verification map whose entries identify the invoked Action and rollback assertion contract, WebDriver profile selection and `verify`; depends: T001-001, T001-003; requirements: NFR-001-INT-01, NFR-001-LOG-01, NFR-001-COMPAT-01; tests first: `tests/Architecture/DevelopmentContractTest.php`, `tests/Architecture/WriteRollbackCoverageTest.php`; validate: `composer test:static`; expected: case-insensitive whole-suite reset/test bypass detection, per-test-method observable-assertion enforcement for both `test_*` and PHPUnit `#[Test]` methods plus Pest closures, uncovered domain writes, rollback methods that do not invoke their mapped Action, incomplete rollback assertions, float authority, absent runtime browser/version capabilities and incomplete approved browser profiles are blocking; forbidden: `RefreshDatabase`, `DatabaseTruncation`, `migrate:fresh`, `db:wipe`, unscoped truncate, empty attribute/named/Pest test method, isolated `Action::class` reference treated as invocation or Chrome-only compatibility claim.

## Phase 2 — Foundational platform persistence and integration

- [X] T001-005 [FND] Create exact platform migrations/models, `PlatformSettingSeeder` and shared audit write contract listed in `../../docs/replatform/task-execution/path-overrides.md`; symbols: singleton `PlatformSetting`, integer `audit_retention_months` constrained 1–120/default 24, idempotent default singleton seed, active `User`, append-only `AuditEvent` with instance and every update-capable Eloquent bulk primitive denied, application-code query-base escape detection, `AuditProperties`/`AuditRecorder` as the sole ordinary write-side audit entry with recursive sensitive-key/type rejection and 16,384-byte encoded-size bound, native-compatible database notification persistence, nullable race-safe dedup key and lock versions, plus the forward-only notification correction migration required when the original migration has already been applied; depends: T001-003; requirements: FR-001-007, FR-001-017, FR-001-020, INV-PLT-006, INV-PLT-007; tests first: `tests/Feature/Platform/PlatformSchemaTest.php`, `tests/Architecture/AuditWriteContractTest.php`; validate: `composer test:prepare && php artisan test tests/Feature/Platform/PlatformSchemaTest.php tests/Architecture/AuditWriteContractTest.php`; expected: forward migrations, idempotent default singleton, bounded retention, no ordinary audit update/delete/upsert/increment/decrement/query-base escape path, bounded minimized write properties, native database notification insert compatibility and unique non-null occurrence deduplication on both fresh and already-migrated databases; forbidden: generic JSON settings, out-of-range retention, direct application AuditEvent/raw-table writes outside the shared recorder and later explicitly allowlisted retention Action, sensitive/object/resource/file-byte properties, public retention-delete model escape hatch, manual-only schema reconciliation or per-tenant retention.
- [X] T001-006 [FND] Configure package authorization in exact files from `../../docs/replatform/task-execution/path-overrides.md`; symbols: teams `tenant_id`, protected global Administrator receiving the complete stable permission catalogue, reserved non-Tenant platform assignment scope `0`, original-primary-key-bound persisted identity and context-restoring `PlatformAdministrator` boundary, environment-provisioned `PlatformAdministratorSeeder`, demo-free root `DatabaseSeeder`, and Editor/Viewer seeds; depends: T001-001, T001-005; requirements: FR-001-003, FR-001-008, FR-001-010, NFR-001-SEC-01, INV-PLT-005; tests first: `tests/Feature/Authorization/PermissionCatalogueTest.php`; validate: `php artisan test tests/Feature/Authorization/PermissionCatalogueTest.php`; expected: idempotent catalogue matches documentation, a new tenantless active Administrator can be provisioned without default credentials, a repeated run may update only the already assigned protected Administrator while preserving its hash, every pre-existing tenantless non-Administrator or tenant-user email collision fails closed without modifying or elevating that account, force-filled/stale in-memory User primary-key/tenant/active attributes cannot impersonate or elevate another identity or bypass the persisted active tenantless identity requirement, and the boundary permits protected plus otherwise valid tenant abilities independently of the current permission-team value without leaking or changing it; every tenant operation still passes its policy/invariant checks and protected abilities cannot be tenant-assigned; forbidden: trusting a mutable current model key or mutable User tenant/active attributes, role-name business branches outside the protected boundary, direct-user permission UI, runtime generation, demo/default/embedded Administrator credentials, taking over or elevating any pre-existing non-Administrator, a persisted Tenant with ID `0`, or super-admin/invariant bypass.
- [X] T001-007 [FND] Integrate active-user, correlation-ID and Feature 007 context middleware in `app/Http/Middleware/EnsureActiveUser.php`, `app/Http/Middleware/AssignCorrelationId.php`, `app/Support/Diagnostics/CorrelationId.php`, `bootstrap/app.php`, `app/Providers/AppServiceProvider.php`; symbols: request-scoped `TenantContext` resolved only from its request attribute, request/response/exception-log UUID-v4 `X-Correlation-ID`, scoped/log cleanup, exact tenant middleware aliases/priority before `SubstituteBindings`, authentication/active-user deny; depends: T001-005, T001-006, T007-003; requirements: FR-001-002, FR-001-009, FR-001-012, NFR-001-LOG-01, INV-PLT-001, INV-CTX-001; tests first: `tests/Feature/Tenancy/TenantContextMiddlewareTest.php`, `tests/Feature/Diagnostics/CorrelationIdTest.php`; validate: `php artisan test tests/Feature/Tenancy/TenantContextMiddlewareTest.php tests/Feature/Diagnostics/CorrelationIdTest.php`; expected: one context implementation, one lowercase UUID-v4 runtime correlation value across request/container/normal-or-error response/log context, a valid inbound UUID-v4 preserved, invalid inbound value replaced rather than trusted/echoed, no cross-request context/log leakage, and unauthenticated/inactive-user denial before tenant resolution; forbidden: duplicate context, unscoped/session fallback in the provider, impersonation, trusting an invalid inbound identifier, invalid session reuse, removing correlation log context before exception reporting, or middleware ordering after route binding.

## Phase 3 — US-001-01 Authenticate (P1, MVP)

**Independent test:** active Administrator reaches global shell; active tenant user reaches own tenant; invalid credentials, deactivated user and inactive tenant deny.

- [X] T001-008 [US1] Write `tests/Feature/Auth/AuthenticationTest.php`, `tests/Feature/Auth/LogoutSessionScopeTest.php`, `tests/Feature/Auth/NoPasswordRecoveryRouteTest.php`; symbols: success/failure, active-state/tenant denial, ordinary logout revokes current session and preserves other database sessions plus the account-wide remember token, password change/reset all-session separation and absent public recovery routes; depends: T001-007; requirements: FR-001-001, FR-001-002, FR-001-012, FR-001-014, INV-PLT-001; validate: `php artisan test tests/Feature/Auth/AuthenticationTest.php tests/Feature/Auth/LogoutSessionScopeTest.php tests/Feature/Auth/NoPasswordRecoveryRouteTest.php`; expected before implementation: focused failures; forbidden: ordinary all-session/remember-token logout, mail self-reset, public registration or navigation-only authorization assertion.
- [X] T001-009 [US1] Configure Filament auth/panel in `app/Providers/Filament/AdminPanelProvider.php`, `app/Http/Controllers/Auth/LogoutController.php`, `bootstrap/providers.php`, `config/auth.php`, `app/Models/User.php`; symbols: provider registration/controller binding, `canAccessPanel()`, local email/password, session regeneration and `logoutCurrentDevice()` ordinary logout with current-session/CSRF rotation; depends: T001-008; requirements: FR-001-001, FR-001-002, FR-001-014; tests first: T001-008; validate: same command as T001-008; expected: authentication grants no business ability and logout preserves other sessions plus their remember-cookie validity; forbidden: Filament native account-wide remember-token rotation, all-session ordinary logout, social login, magic link or middleware bypass.

## Phase 4 — US-001-02 Permission-aware shell (P1)

**Independent test:** granted navigation appears, direct unauthorized route denies, and tenant context remains visible.

- [X] T001-010 [P] [US2] Write `tests/Feature/Authorization/NavigationPolicyTest.php`, `tests/Livewire/Shell/TenantContextIndicatorTest.php`, `tests/Browser/Shell/AccessibilityCompatibilityTest.php`; symbols: permission navigation, direct-route deny, active-user protection on every authenticated panel route, Livewire persistence allowlist for active-user and tenant route middleware, visible global/tenant context, executable WCAG 2.2 AA keyboard/focus/labels/contrast/errors checks and complete 360/768/1280 seven-profile browser contract; depends: T001-009, T007-006; requirements: FR-001-004, FR-001-007, FR-001-009, NFR-001-A11Y-01, NFR-001-COMPAT-01, INV-PLT-002; validate: exact T001-010–T001-011 structural command in `../../docs/replatform/task-execution/feature-001.md`; expected before implementation: focused failures; forbidden: manually injected context as sole tenant-bound HTTP evidence, role-name assertions, mouse-only control, vague responsive assertion or hidden navigation treated as authorization; real seven-profile execution remains exclusively T001-024-owned.
- [X] T001-011 [US2] Integrate shell hooks in `app/Providers/Filament/AdminPanelProvider.php`, `app/Providers/AuthServiceProvider.php`, `resources/views/filament/components/global-context.blade.php`; symbols: policy navigation, panel-wide active-user auth middleware, Livewire persistence registration for active-user plus tenant-only context/team/active/presentation middleware, global/tenant shell distinction, Feature 007 indicator rendering and WCAG/browser-ready behavior; depends: T001-010, T007-012; requirements: FR-001-004, FR-001-007, FR-001-009, NFR-001-A11Y-01, NFR-001-COMPAT-01; tests first: T001-010; validate: same structural commands as T001-010; expected: one accessible responsive indicator/shell implementation while the global dashboard and tenant administration remain available without selected tenant; forbidden: panel-wide required-tenant middleware, non-persistent Livewire security middleware, duplicate component, Preline, SPA shell, mouse-only operation, client-only authorization or claiming the T001-024 browser matrix before it runs.

## Phase 5 — US-001-03 Administrator identity/role integration (P1)

**Independent test:** Feature 007 resources are Administrator-only, protected abilities remain hidden/denied, and password reset is secret-safe.

- [ ] T001-012 [P] [US3] Write `tests/Feature/IdentityAccess/PlatformIdentityIntegrationTest.php`, `tests/Feature/IdentityAccess/PasswordAdministrationTest.php`; symbols: single Resource registration, runtime reset table-Action delegation, direct persisted Administrator/context/target anti-spoof authorization, same-tenant target, one observable hash, shared all-session invalidation including remember-token rotation, unchanged identity/lifecycle/tenant roles, exact minimized reset audit, secret absence from materialized stores/logs and observable rollback; depends: T001-006, T007-009, T007-010; requirements: FR-001-003, FR-001-008, FR-001-010, FR-001-013, FR-001-015, FR-001-017, FR-007-018, FR-007-019, INV-PLT-005, INV-PLT-006; validate: focused identity tests; expected before integration: focused failures attributable only to T001-013/T001-014; forbidden: static-only adapter proof, duplicate lifecycle/session writes, password value exposure, unprotected direct Action, preserved remember token or cross-tenant reset.
- [X] T001-013 [US3] Implement `app/Domain/IdentityAccess/Actions/ResetTenantUserPassword.php`, `app/Domain/IdentityAccess/Actions/InvalidateUserSessions.php` and their two exact entries in `tests/Architecture/Fixtures/domain-write-rollback-map.php`; symbols: persisted Administrator/context/target authorization, same-tenant target, exactly one hash, mandatory delegation to the shared invalidator, deletion of all target database sessions plus remember-token rotation, exact `tenant.user.password-reset` empty-properties audit, secret-free materialized observables and observable rollback on audit/session-write failure; depends: T001-012; requirements: FR-001-008, FR-001-015, FR-001-017, FR-007-018, NFR-001-INT-01; tests first: T001-012; validate: exact T001-013 command in `../../docs/replatform/task-execution/feature-001.md`; expected: transactional secret-safe reset with both domain writes covered by the rollback architecture gate and expected write count raised by two; forbidden: user lifecycle/role mutation, plaintext/hash/token/session logging or metadata, duplicated direct session deletion, platform-role mutation, missing rollback-map coverage or non-observable rollback assertion.
- [X] T001-014 [US3] Integrate Feature 007 resources and reset Action in the exact provider, Users table and adapter paths listed in `../../docs/replatform/task-execution/path-overrides.md`; symbols: one Administrator navigation registration and executable password-reset table Action delegating to the domain Action; depends: T001-011, T001-013, T007-010; requirements: FR-001-003, FR-001-008, FR-001-015, FR-007-018; tests first: T001-012; validate: identity integration tests; expected: one User/Role resource implementation and one wired reset action; forbidden: unused adapter, duplicate resources, direct model/session/audit write or static-only delegation evidence.

## Phase 6 — US-001-04 User changes own password (P2)

- [ ] T001-015 [P] [US4] Write `tests/Feature/IdentityAccess/ChangeOwnPasswordTest.php`, `tests/Feature/Console/ResetAdministratorPasswordCommandTest.php`; symbols: current-password, hidden input, hashing, session invalidation, no secret audit; depends: T001-013; requirements: FR-001-014–FR-001-017, FR-007-018, FR-007-019, INV-PLT-006; validate: focused password tests; expected before implementation: focused failures; forbidden: reset token/email or plaintext console capture.
- [ ] T001-016 [US4] Implement `app/Domain/IdentityAccess/Actions/ChangeOwnPassword.php`, `app/Console/Commands/ResetAdministratorPasswordCommand.php`, `app/Filament/Pages/Profile.php`; symbols: authenticated self-change and interactive emergency reset; depends: T001-015; requirements: FR-001-015–FR-001-017, FR-007-018, FR-007-019; tests first: T001-015; validate: same tests; expected: session invalidation without public recovery; forbidden: default password or secret side-channel.

## Phase 7 — US-001-05 Notification delivery and scheduler registration (P2)

- [ ] T001-017 [P] [US5] Write `tests/Feature/Notifications/NotificationDeduplicationTest.php`, `tests/Feature/Notifications/SynchronousMailFailureTest.php`; symbols: recipient/event dedup, database-first persistence and explicit mail result; depends: T001-005, T001-006; requirements: FR-001-018, INV-PLT-004; validate: `php artisan test tests/Feature/Notifications/NotificationDeduplicationTest.php tests/Feature/Notifications/SynchronousMailFailureTest.php`; expected before implementation: focused failures; forbidden: `ShouldQueue`, retry loop, Redis/WebSocket or swallowed mail failure.
- [ ] T001-018 [US5] Implement `app/Domain/Notifications/Actions/DeliverSynchronousNotification.php`, `app/Domain/Notifications/Data/NotificationOccurrence.php`, `app/Models/DatabaseNotification.php`; symbols: deterministic dedup and explicit database/mail result; depends: T001-017; requirements: FR-001-018; tests first: T001-017; validate: same tests; expected: reusable delivery primitive independent of feature commands and scheduler registration; forbidden: `routes/console.php` registration, business-specific queries, hidden retry or secret payload.
- [ ] T001-025 [US5] Write `tests/Feature/Scheduler/SchedulerRegistrationTest.php` and register `contracts:check-renewals`, operation-failure checks and `audit:prune` in `routes/console.php`; symbols: one cron-compatible schedule, overlap locks and explicit command ownership; depends: T001-018, T001-020, T004-020, T006-014; requirements: FR-001-006, FR-001-018, INV-PLT-004; validate: `php artisan schedule:list && php artisan test tests/Feature/Scheduler/SchedulerRegistrationTest.php`; expected: scheduler registration occurs only after feature commands exist; forbidden: business logic in scheduler closures, circular dependency or permanent worker.

## Phase 8 — US-001-06 Platform settings, retention and audit view (P2)

- [ ] T001-019 [P] [US6] Write `tests/Feature/Platform/PlatformSettingTest.php`, `tests/Feature/Audit/AuditRetentionTest.php`, `tests/Livewire/Platform/PlatformSettingPageTest.php`; symbols: integer 1/120 boundaries, reject 0/121/non-integer, default 24, authorization, stale lock, reinforced generic lowering warning with no cutoff/count preview, bounded prune, and TEST-007-010 protected-record assertions; depends: T001-005, T001-007; requirements: FR-001-019–FR-001-021, FR-007-017, FR-007-023, INV-PLT-007, INV-TEN-010; validate: focused platform/audit tests; expected before implementation: focused failures; forbidden: unbounded/per-tenant retention, cutoff/count preview, implicit immediate prune, current-business deletion, revision-identity deletion or named-BudgetVersion deletion.
- [ ] T001-020 [US6] Implement `app/Domain/Platform/Actions/UpdatePlatformSettings.php`, `app/Domain/Audit/Actions/PruneExpiredAuditEvents.php`, `app/Console/Commands/AuditPruneCommand.php`, `app/Policies/PlatformSettingPolicy.php` and add only the exact prune Action path to `tests/Architecture/AuditWriteContractTest.php` retention allowlist; symbols: singleton lock, integer 1–120 validation/default 24, current cutoff, audit-event-only ID batches, explicit protected-record exclusion and stable errors; depends: T001-019; requirements: FR-001-019–FR-001-021, FR-007-017, FR-007-023, INV-TEN-010; tests first: T001-019; validate: focused platform/audit tests plus `php artisan test tests/Architecture/AuditWriteContractTest.php`; expected: explicit bounded settings update and pruning limited to eligible expired audit events through the sole named query-base/delete exception; forbidden: any broader raw-write allowlist, observer prune, `expires_at`, retry, generic settings service or deletion of business rows, revision identity or named BudgetVersion rows.
- [ ] T001-021 [US6] Implement `app/Filament/Pages/PlatformSettings.php`, `resources/views/filament/pages/platform-settings.blade.php`; symbols: integer 1–120 typed form, lock and generic lowering confirmation without cutoff/eligible-count preview; depends: T001-020; requirements: FR-001-019, FR-001-020, FR-007-017, FR-007-023; tests first: T001-019; validate: platform settings Livewire test; expected: save/prune separation and exact approved warning; forbidden: cutoff/count preview, generic key editor or tenant-role access.
- [ ] T001-026 [P] [US6] Write `tests/Feature/Audit/AuditViewAuthorizationTest.php`, `tests/Feature/Audit/AuditViewMinimizationTest.php`, `tests/Livewire/Audit/AuditLogPageTest.php`; symbols: same-tenant `audit.view`, Administrator tenant/global scope, minimized properties and no export route; depends: T001-005, T007-012; requirements: FR-001-007, FR-001-017, FR-007-017, Q-020, INV-PLT-006, INV-TEN-006; validate: focused audit-view tests; expected before implementation: focused failures; forbidden: audit export, secret/file payload or cross-tenant existence leak.
- [ ] T001-027 [US6] Implement `app/Domain/Audit/Queries/AuditEventListQuery.php`, `app/Policies/AuditEventPolicy.php`, `app/Filament/Pages/AuditLogPage.php`, `resources/views/filament/pages/audit-log-page.blade.php`; symbols: tenant/global filters, bounded pagination, minimized DTO and no download action; depends: T001-026; requirements: FR-001-007, FR-001-017, FR-007-017, Q-020; tests first: T001-026; validate: focused audit-view tests; expected: permission-controlled tenant/global audit view with no export; forbidden: raw payload rendering, Eloquent model exposure or export endpoint.

## Phase 9 — Shared quality and release infrastructure

- [ ] T001-022 [P] [FND] Create `.github/workflows/quality.yml`; symbols: static/accounting/application, latest-two Chrome/Edge/Firefox plus current-Safari browser matrix, MySQL 8.4 jobs, explicit supported runner/profile and lock-keyed caches; depends: T001-004; requirements: FR-001-005, NFR-001-COMPAT-01, INV-PLT-003; tests first: `tests/Architecture/WorkflowContractTest.php`; validate: workflow contract test; expected: mandatory gates on PR/protected branch and recorded resolved browser versions; forbidden: optional mandatory jobs, unpinned actions, destructive DB reset or unexecuted Safari claim.
- [ ] T001-023 [FND] Create `.github/workflows/release.yml`, `scripts/build-release.sh`, `tests/Feature/Deployment/ReleaseArtifactContractTest.php`; symbols: verified commit, production dependencies/assets, ZIP/checksum/provenance; depends: T001-002, T001-022; requirements: FR-001-005, INV-PLT-003; tests first: release contract test; validate: release contract plus `bash scripts/build-release.sh --verify-only`; expected: immutable host-ready ZIP; forbidden: production rebuild, `.env`, test data, `node_modules` or mutable latest artifact.

## Phase 10 — Verification

- [ ] T001-024 [VER] Run Feature 001 verification and record actual results in every exact documentation path listed in `../../docs/replatform/task-execution/path-overrides.md`; depends: T001-009, T001-011, T001-014, T001-016, T001-018, T001-021, T001-023, T001-025, T001-027; requirements: FR-001-001–FR-001-021, NFR-001-SEC-01, NFR-001-INT-01, NFR-001-LOG-01, NFR-001-A11Y-01, NFR-001-COMPAT-01, NFR-001-MAINT-01, FR-007-017–FR-007-019, FR-007-023; validate: exact T001-024 command in `../../docs/replatform/task-execution/feature-001.md`; expected: mandatory suites, current-session logout, bounded retention and approved WCAG/browser Dusk matrix pass; forbidden: skipped checks, unexecuted claims or weakened tests.

## Dependencies and execution order

`T001-001 → {T001-002,T001-003}; T001-003 → {T001-004,T001-005}; T001-005 → T001-006 → T007-003 → T001-007 → US1 → US2/US3 → US4/US5/US6`; T004-020 and T006-014 precede T001-025; T001-022/T001-023 provide shared quality/release infrastructure; T001-024 is the final verification task.

## MVP scope

T001-001–T001-009 plus Feature 007 context foundation provide reproducible scaffold, safe tests and authentication. No fake single-tenant fallback is permitted.
